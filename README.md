# support-analytics-api
This service ingests customer support events, processes them asynchronously, stores them in a relational database, and exposes analytics and search APIs with authentication, tests, and CI
